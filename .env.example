# ============================================================
# ImageAI 环境变量配置示例
# 复制此文件为 .env.local 并填入真实值
# ============================================================
# ⚠️ 安全提示：
# - 此文件作为模板提交到 Git，不包含真实密钥
# - 真实密钥请填入 .env.local（已被 .gitignore 忽略）
# - 生产环境在云平台控制台手动配置环境变量
# ============================================================

# ============================================================
# 基础配置 (必需)
# ============================================================
NODE_ENV=development

# ============================================================
# 数据库配置 (必需)
# ============================================================
# 本地开发使用 SQLite:
DATABASE_URL="file:./prisma/dev.db"
#
# 生产环境使用 PostgreSQL (Neon/Supabase/RDS):
# Neon 连接字符串格式:
# DATABASE_URL="postgresql://user:password@host.neon.tech/database?sslmode=require"
#
# 从 Vercel 获取 Neon 连接字符串:
# 1. 登录 Vercel 控制台 → 你的项目 → Storage → Neon
# 2. 点击 ".env.local" 标签
# 3. 复制 DATABASE_URL 的值

# ============================================================
# JWT 认证配置 (必需)
# ============================================================
# 生成强随机密钥命令:
# Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 } | ForEach-Object { [byte]$_ }))
JWT_SECRET="your-super-secret-jwt-key-change-this"
JWT_EXPIRES_IN="7d"

# ============================================================
# AI 图像生成配置 (至少配置一个)
# ============================================================
# 通义千问 (推荐 - 国内可用)
# 从阿里云 DashScope 获取: https://dashscope.console.aliyun.com/apiKey
TONGYI_API_KEY="your-tongyi-api-key"

# Gemini (备用 - 需要代理)
# 从 Google AI Studio 获取: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY="your-gemini-api-key"

# 代理配置（仅 Gemini 需要）
# HTTP_PROXY=http://127.0.0.1:31181
# HTTPS_PROXY=http://127.0.0.1:31181

# ============================================================
# 文件存储配置 (可选 - 至少配置一种用于生产)
# ============================================================
# --- AWS S3 配置 ---
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="ap-northeast-1"
AWS_S3_BUCKET_NAME=""

# --- 阿里云 OSS 配置 (推荐用于阿里云部署) ---
ALIYUN_ACCESS_KEY_ID=""
ALIYUN_ACCESS_KEY_SECRET=""
ALIYUN_OSS_REGION="oss-cn-hangzhou"
ALIYUN_OSS_BUCKET_NAME=""

# ============================================================
# 邮件服务配置 (可选)
# ============================================================
# Resend (推荐): https://resend.com
RESEND_API_KEY=""
FROM_EMAIL="onboarding@resend.dev"
FROM_NAME="ImageAI"

# ============================================================
# 应用配置
# ============================================================
# 本地开发
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# 生产环境 (部署后更新为实际域名)
# NEXT_PUBLIC_APP_URL="https://your-domain.com"

NEXT_PUBLIC_APP_NAME="ImageAI"

# ============================================================
# 功能开关
# ============================================================
ENABLE_REGISTRATION="true"
ENABLE_SOCIAL_SHARE="true"
ENABLE_POSE_GENERATION="true"
ENABLE_PAYMENT="false"

# ============================================================
# 限制配置
# ============================================================
MAX_FILE_SIZE="10"
MAX_BATCH_GENERATE="4"
CONCURRENT_GENERATION="2"

# ============================================================
# 开发配置
# ============================================================
LOG_LEVEL="debug"
