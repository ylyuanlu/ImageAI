"use client";

import React, { useState, useEffect, Suspense } from 'react';
import { useSearchParams } from 'next/navigation';
import Link from 'next/link';
import { getImageData } from '@/lib/imageStore';

/**
 * 结果页面内容组件
 * 使用 useSearchParams 需要在 Suspense 边界内
 */
function ResultContent() {
  const searchParams = useSearchParams();
  const [copied, setCopied] = useState(false);
  const [sourceImages, setSourceImages] = useState<{model?: string; outfit?: string}>({});
  const [selectedImageIndex, setSelectedImageIndex] = useState(0);
  const [downloadFilename, setDownloadFilename] = useState<string>('');

  // 在客户端生成下载文件名，避免 hydration mismatch
  useEffect(() => {
    setDownloadFilename(`ai-outfit-${Date.now()}-${selectedImageIndex + 1}.jpg`);
  }, [selectedImageIndex]);

  // 获取URL参数
  const imageParam = searchParams.get('image');
  const imagesParam = searchParams.get('images');
  const pose = searchParams.get('pose') || '正面站立';
  const style = searchParams.get('style') || 'street';
  const time = searchParams.get('time') || '0';
  const countParam = searchParams.get('count') || '1';
  const modelFromUrl = searchParams.get('model');
  const outfitFromUrl = searchParams.get('outfit');

  // 解析生成的图片列表
  const generatedImages: string[] = React.useMemo(() => {
    if (imagesParam) {
      try {
        return JSON.parse(imagesParam);
      } catch {
        return imageParam ? [imageParam] : [];
      }
    }
    return imageParam ? [imageParam] : [];
  }, [imagesParam, imageParam]);

  const generateCount = parseInt(countParam, 10) || generatedImages.length || 1;

  // 从 IndexedDB、localStorage 或 URL 参数读取源图片数据
  useEffect(() => {
    const loadSourceImages = async () => {
      const fromHistory = searchParams.get('from') === 'history';
      const storeKey = searchParams.get('key');
      
      if (fromHistory && storeKey) {
        // 新版本：从 IndexedDB 读取
        try {
          const imageData = await getImageData(storeKey);
          if (imageData) {
            setSourceImages({
              model: imageData.model || undefined,
              outfit: imageData.outfits[0] || undefined
            });
          }
        } catch (e) {
          console.error('从 IndexedDB 读取历史记录数据失败:', e);
        }
        
        // 兼容旧版本：尝试从 localStorage 读取
        const historyData = localStorage.getItem('historyResultData');
        if (historyData) {
          try {
            const data = JSON.parse(historyData);
            setSourceImages({
              model: data.model || undefined,
              outfit: data.outfit || undefined
            });
            localStorage.removeItem('historyResultData');
          } catch (e) {
            console.error('解析历史记录数据失败:', e);
          }
        }
      } else if (modelFromUrl || outfitFromUrl) {
        setSourceImages({
          model: modelFromUrl || undefined,
          outfit: outfitFromUrl || undefined
        });
      } else {
        // 首先尝试从 sessionStorage 读取
        const savedImagesFromSession = sessionStorage.getItem('resultSourceImages');
        if (savedImagesFromSession) {
          try {
            const images = JSON.parse(savedImagesFromSession);
            setSourceImages(images);
          } catch (e) {
            console.error('解析源图片数据失败:', e);
          }
        } else {
          // 兼容旧版本：尝试从 localStorage 读取
          const savedImages = localStorage.getItem('resultSourceImages');
          if (savedImages) {
            try {
              const images = JSON.parse(savedImages);
              setSourceImages(images);
            } catch (e) {
              console.error('解析源图片数据失败:', e);
            }
          }
        }
      }
    };
    
    loadSourceImages();
  }, [searchParams, modelFromUrl, outfitFromUrl]);

  const modelImage = sourceImages.model;
  const outfitImage = sourceImages.outfit;
  const currentImage = generatedImages[selectedImageIndex] || generatedImages[0];

  // 复制到剪贴板
  const handleCopy = () => {
    if (currentImage) {
      navigator.clipboard.writeText(currentImage);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    }
  };

  // 社交分享
  const handleShare = (platform: string) => {
    const text = encodeURIComponent('AI试衣间生成的穿搭效果图，太惊艳了！✨');
    const url = encodeURIComponent(window.location.href);
    
    switch (platform) {
      case 'wechat':
        navigator.clipboard.writeText(`AI试衣间生成的穿搭效果图！\n${currentImage}`);
        alert('图片链接已复制，请粘贴到微信分享');
        break;
      case 'xiaohongshu':
        navigator.clipboard.writeText(`用AI试衣间生成的穿搭效果图，太惊艳了！✨ #AI试衣间 #虚拟穿搭\n${currentImage}`);
        alert('文案已复制，请粘贴到小红书分享');
        break;
      case 'instagram':
        navigator.clipboard.writeText(`Outfit generated by AI Fitting Room! ✨ #AIFitting #VirtualTryOn\n${currentImage}`);
        alert('Caption copied! Paste to Instagram.');
        break;
      case 'twitter':
        window.open(`https://twitter.com/intent/tweet?text=${text}&url=${url}`, '_blank');
        break;
    }
  };

  // 如果没有图片，显示空状态
  if (!generatedImages.length) {
    return (
      <div className="min-h-screen bg-gray-50 pt-24 px-6 pb-12">
        <div className="max-w-2xl mx-auto text-center">
          <div className="card p-8">
            <div className="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center mx-auto mb-6">
              <svg className="w-10 h-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            </div>
            <h3 className="text-xl font-bold text-gray-900 mb-2">暂无生成结果</h3>
            <p className="text-gray-500 mb-6">请前往开始创作页面生成您的专属穿搭效果图</p>
            <Link
              href="/upload"
              className="px-6 py-3 bg-primary-600 text-white rounded-xl font-medium hover:bg-primary-700 transition-colors inline-block"
            >
              开始创作
            </Link>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gray-50 pt-24 px-6 pb-12">
      <div className="max-w-4xl mx-auto">
        {/* 成功提示 */}
        <div className="text-center mb-8">
          <div className="inline-flex items-center gap-2 px-4 py-2 bg-green-100 text-green-700 rounded-full mb-4">
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
            </svg>
            <span className="font-medium">
              {generateCount > 1 ? `生成成功！共 ${generateCount} 张` : '生成成功！'}
            </span>
          </div>
          <h1 className="font-heading text-3xl md:text-4xl font-bold text-gray-900 mb-2">
            您的专属穿搭效果图
          </h1>
          <p className="text-gray-600">
            耗时 {time} 秒 · 已保存到历史记录
          </p>
        </div>

        {/* 主结果区域 */}
        <div className="card p-8 mb-8">
          {/* 大图展示 */}
          <div className="relative rounded-2xl overflow-hidden shadow-lg mb-8 bg-gray-100">
            <img
              src={currentImage}
              alt="生成的穿搭效果图"
              className="w-full h-auto"
            />
            {/* 水印 */}
            <div className="absolute bottom-4 right-4 px-3 py-1.5 bg-black/50 backdrop-blur-sm rounded-full">
              <span className="text-white text-xs font-medium">AI试衣间</span>
            </div>
            {/* 图片计数器（批量生成时显示） */}
            {generateCount > 1 && (
              <div className="absolute top-4 right-4 px-3 py-1.5 bg-black/50 backdrop-blur-sm rounded-full">
                <span className="text-white text-sm font-medium">
                  {selectedImageIndex + 1} / {generateCount}
                </span>
              </div>
            )}
          </div>

          {/* 批量生成图片缩略图选择器 */}
          {generateCount > 1 && generatedImages.length > 1 && (
            <div className="mb-8">
              <p className="text-sm text-gray-600 mb-3 text-center">选择查看不同的生成结果</p>
              <div className="flex justify-center gap-3 flex-wrap">
                {generatedImages.map((img, index) => (
                  <button
                    key={index}
                    onClick={() => setSelectedImageIndex(index)}
                    className={`relative rounded-xl overflow-hidden transition-all ${
                      selectedImageIndex === index
                        ? 'ring-2 ring-primary-500 ring-offset-2 shadow-md'
                        : 'opacity-70 hover:opacity-100'
                    }`}
                  >
                    <img
                      src={img}
                      alt={`生成结果 ${index + 1}`}
                      className="w-20 h-20 object-cover"
                    />
                    {selectedImageIndex === index && (
                      <div className="absolute inset-0 bg-primary-500/20 flex items-center justify-center">
                        <svg className="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
                        </svg>
                      </div>
                    )}
                  </button>
                ))}
              </div>
            </div>
          )}

          {/* 对比区域 */}
          {(modelImage || outfitImage) && (
            <div className="grid grid-cols-2 gap-4 mb-8 p-4 bg-gray-50 rounded-xl">
              {modelImage && (
                <div>
                  <p className="text-xs text-gray-500 mb-2 text-center">原始模特</p>
                  <div className="aspect-[3/4] rounded-lg overflow-hidden">
                    <img src={modelImage} alt="模特" className="w-full h-full object-cover" />
                  </div>
                </div>
              )}
              {outfitImage && (
                <div>
                  <p className="text-xs text-gray-500 mb-2 text-center">参考服装</p>
                  <div className="aspect-[3/4] rounded-lg overflow-hidden">
                    <img src={outfitImage} alt="服装" className="w-full h-full object-cover" />
                  </div>
                </div>
              )}
            </div>
          )}

          {/* 操作按钮组 */}
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
            <a
              href={currentImage}
              download={downloadFilename || `ai-outfit-${selectedImageIndex + 1}.jpg`}
              className="flex flex-col items-center gap-2 p-4 bg-primary-50 rounded-xl hover:bg-primary-100 transition-colors group"
            >
              <div className="w-12 h-12 rounded-full bg-primary-100 group-hover:bg-primary-200 flex items-center justify-center transition-colors">
                <svg className="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                </svg>
              </div>
              <span className="text-sm font-medium text-primary-700">下载图片</span>
            </a>

            <button
              onClick={handleCopy}
              className="flex flex-col items-center gap-2 p-4 bg-gray-50 rounded-xl hover:bg-gray-100 transition-colors group"
            >
              <div className="w-12 h-12 rounded-full bg-gray-100 group-hover:bg-gray-200 flex items-center justify-center transition-colors">
                {copied ? (
                  <svg className="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
                  </svg>
                ) : (
                  <svg className="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                  </svg>
                )}
              </div>
              <span className="text-sm font-medium text-gray-700">
                {copied ? '已复制' : '复制链接'}
              </span>
            </button>

            <Link
              href="/upload"
              className="flex flex-col items-center gap-2 p-4 bg-gray-50 rounded-xl hover:bg-gray-100 transition-colors group"
            >
              <div className="w-12 h-12 rounded-full bg-gray-100 group-hover:bg-gray-200 flex items-center justify-center transition-colors">
                <svg className="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                </svg>
              </div>
              <span className="text-sm font-medium text-gray-700">再生成一张</span>
            </Link>

            <Link
              href="/history"
              className="flex flex-col items-center gap-2 p-4 bg-gray-50 rounded-xl hover:bg-gray-100 transition-colors group"
            >
              <div className="w-12 h-12 rounded-full bg-gray-100 group-hover:bg-gray-200 flex items-center justify-center transition-colors">
                <svg className="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <span className="text-sm font-medium text-gray-700">查看历史</span>
            </Link>
          </div>

          {/* 社交分享区域 */}
          <div className="border-t border-gray-200 pt-8">
            <h3 className="text-center text-gray-900 font-medium mb-6">分享到社交平台</h3>
            <div className="flex justify-center gap-4">
              <button
                onClick={() => handleShare('wechat')}
                className="group flex flex-col items-center gap-2"
              >
                <div className="w-14 h-14 rounded-full bg-green-500 text-white flex items-center justify-center shadow-md group-hover:shadow-lg group-hover:scale-110 transition-all">
                  <svg className="w-7 h-7" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M8.5,14.5c-3.6,0-6.5-2.5-6.5-5.5s2.9-5.5,6.5-5.5s6.5,2.5,6.5,5.5S12.1,14.5,8.5,14.5z M17.5,13.5c-0.8,0-1.6,0.2-2.3,0.5c0.2,0.6,0.3,1.2,0.3,1.9c0,3-2.9,5.5-6.5,5.5c-0.8,0-1.6-0.1-2.3-0.3l-3.2,1.8l0.9-2.6C2.5,19.2,1,17.5,1,15.5c0-3.6,3.4-6.5,7.5-6.5c0.5,0,0.9,0,1.4,0.1c0.8-2.7,3.8-4.6,7.1-4.1c3.1,0.5,5.3,3,5,5.6C22,13.3,20,15,17.5,13.5z"/>
                  </svg>
                </div>
                <span className="text-xs text-gray-600">微信</span>
              </button>

              <button
                onClick={() => handleShare('xiaohongshu')}
                className="group flex flex-col items-center gap-2"
              >
                <div className="w-14 h-14 rounded-full bg-red-500 text-white flex items-center justify-center shadow-md group-hover:shadow-lg group-hover:scale-110 transition-all">
                  <svg className="w-7 h-7" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                  </svg>
                </div>
                <span className="text-xs text-gray-600">小红书</span>
              </button>

              <button
                onClick={() => handleShare('instagram')}
                className="group flex flex-col items-center gap-2"
              >
                <div className="w-14 h-14 rounded-full bg-gradient-to-br from-purple-600 via-pink-500 to-orange-400 text-white flex items-center justify-center shadow-md group-hover:shadow-lg group-hover:scale-110 transition-all">
                  <svg className="w-7 h-7" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                  </svg>
                </div>
                <span className="text-xs text-gray-600">Instagram</span>
              </button>

              <button
                onClick={() => handleShare('twitter')}
                className="group flex flex-col items-center gap-2"
              >
                <div className="w-14 h-14 rounded-full bg-black text-white flex items-center justify-center shadow-md group-hover:shadow-lg group-hover:scale-110 transition-all">
                  <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                  </svg>
                </div>
                <span className="text-xs text-gray-600">Twitter</span>
              </button>
            </div>
          </div>
        </div>

        {/* 推荐操作 */}
        <div className="card p-6">
          <h3 className="font-heading text-lg font-bold text-gray-900 mb-4">接下来您可以</h3>
          <div className="grid md:grid-cols-3 gap-4">
            <Link
              href="/outfit"
              className="flex items-center gap-3 p-4 bg-primary-50 rounded-xl hover:bg-primary-100 transition-colors"
            >
              <div className="w-10 h-10 rounded-lg bg-primary-100 flex items-center justify-center flex-shrink-0">
                <svg className="w-5 h-5 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
                </svg>
              </div>
              <div>
                <p className="font-medium text-gray-900">更换穿搭</p>
                <p className="text-sm text-gray-500">试试其他风格</p>
              </div>
            </Link>

            <Link
              href="/pose"
              className="flex items-center gap-3 p-4 bg-primary-50 rounded-xl hover:bg-primary-100 transition-colors"
            >
              <div className="w-10 h-10 rounded-lg bg-primary-100 flex items-center justify-center flex-shrink-0">
                <svg className="w-5 h-5 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
              </div>
              <div>
                <p className="font-medium text-gray-900">更换姿势</p>
                <p className="text-sm text-gray-500">改变模特姿态</p>
              </div>
            </Link>

            <Link
              href="/upload"
              className="flex items-center gap-3 p-4 bg-primary-50 rounded-xl hover:bg-primary-100 transition-colors"
            >
              <div className="w-10 h-10 rounded-lg bg-primary-100 flex items-center justify-center flex-shrink-0">
                <svg className="w-5 h-5 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <p className="font-medium text-gray-900">重新创作</p>
                <p className="text-sm text-gray-500">上传新图片</p>
              </div>
            </Link>
          </div>
        </div>
      </div>
    </div>
  );
}

/**
 * 加载状态组件
 */
function LoadingState() {
  return (
    <div className="min-h-screen bg-gray-50 pt-24 px-6 pb-12">
      <div className="max-w-4xl mx-auto">
        <div className="text-center mb-8">
          <div className="inline-flex items-center gap-2 px-4 py-2 bg-green-100 text-green-700 rounded-full mb-4">
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
            </svg>
            <span className="font-medium">生成成功！</span>
          </div>
          <h1 className="font-heading text-3xl md:text-4xl font-bold text-gray-900 mb-2">
            您的专属穿搭效果图
          </h1>
          <p className="text-gray-600">正在加载结果...</p>
        </div>
        <div className="card p-8 text-center">
          <div className="w-20 h-20 rounded-full bg-primary-100 flex items-center justify-center mx-auto mb-6">
            <svg className="w-10 h-10 text-primary-600 animate-spin" fill="none" viewBox="0 0 24 24">
              <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
              <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
          </div>
          <p className="text-gray-500">正在加载结果页面...</p>
        </div>
      </div>
    </div>
  );
}

/**
 * 结果页面
 * 使用 Suspense 包装使用 useSearchParams 的组件
 */
export default function ResultPage() {
  return (
    <Suspense fallback={<LoadingState />}>
      <ResultContent />
    </Suspense>
  );
}
